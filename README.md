# Metadata Mover

This plugin moves notes based on different properties in their metadata.

You define the name of the property, the value of the property, and the folder it should be moved to.

If you define the 'up' of the note, it will automatically be moved to the same folder as it's 'up'

Sounds simple enough, so let's see how easy it is to implement in practice.
